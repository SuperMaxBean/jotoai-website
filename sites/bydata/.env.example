# Base URL of the shared admin backend (port 3004 in dev, https://admin.jotoai.com in prod).
# Used by ContactForm to fetch /api/captcha and POST /api/bydata/contact.
# Default (when unset): https://admin.jotoai.com
VITE_API_BASE=https://admin.jotoai.com
